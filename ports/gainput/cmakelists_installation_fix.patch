diff --git a/CMakeLists.txt b/CMakeLists.txt
index a443b66..c18d71a 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -1,3 +1,4 @@
+project(gainput_solution)
 cmake_minimum_required(VERSION 2.8)
 set(GAINPUT_MAJOR_VERSION 1)
 set(GAINPUT_MINOR_VERSION 0)
@@ -13,7 +14,7 @@ if(!WIN32)
 	set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -Wall -pedantic -Wextra")
 else()
 	set(XINPUT "Xinput9_1_0")
-	if ( ${CMAKE_SYSTEM_VERSION} LESS 6.1 )
+	if (CMAKE_SYSTEM_VERSION LESS 6.1 )
 		set(XINPUT, "xinput")
 	endif()
 endif()
diff --git a/lib/CMakeLists.txt b/lib/CMakeLists.txt
index 14d5e85..9a05e9d 100644
--- a/lib/CMakeLists.txt
+++ b/lib/CMakeLists.txt
@@ -27,11 +27,9 @@ if (GAINPUT_BUILD_SHARED)
     FOLDER gainput
   )
   set(install_libs ${install_libs} gainput)
-endif (GAINPUT_BUILD_SHARED)
-
-if (GAINPUT_BUILD_STATIC)
+elseif(GAINPUT_BUILD_STATIC)
   message(STATUS "..Building shared libraries (-DGAINPUT_BUILD_STATIC=OFF to disable)")
-  add_library(gainputstatic STATIC ${sources} ${mmsources})
+  add_library(gainput STATIC ${sources} ${mmsources})
   set_target_properties(gainputstatic PROPERTIES DEBUG_POSTFIX -d FOLDER gainput)
   set(install_libs ${install_libs} gainputstatic)
 endif (GAINPUT_BUILD_STATIC)
@@ -73,7 +71,36 @@ install(
 
 install(
     TARGETS ${install_libs}
+    EXPORT gainput-export
     LIBRARY DESTINATION "${libdir}"
     ARCHIVE DESTINATION "${libdir}"
     RUNTIME DESTINATION "bin"
 )
+
+install(
+        EXPORT gainput-export
+        FILE gainputTargets.cmake
+        NAMESPACE gainput::
+        DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/gainput
+)
+
+include(CMakePackageConfigHelpers)
+configure_package_config_file(
+        config.cmake.in
+        ${CMAKE_CURRENT_BINARY_DIR}/gainputConfig.cmake
+        INSTALL_DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/gainput
+        PATH_VARS GAINPUT_VERSION
+)
+write_basic_package_version_file(
+        ${CMAKE_CURRENT_BINARY_DIR}/gainputConfigVersion.cmake
+        VERSION PROJECT_VERSION
+        COMPATIBILITY SameMajorVersion
+)
+
+install(
+        FILES
+        ${CMAKE_CURRENT_BINARY_DIR}/gainputConfig.cmake
+        ${CMAKE_CURRENT_BINARY_DIR}/gainputConfigVersion.cmake
+        DESTINATION
+        ${CMAKE_INSTALL_LIBDIR}/cmake/gainput
+)
\ No newline at end of file
diff --git a/lib/config.cmake.in b/lib/config.cmake.in
new file mode 100644
index 0000000..af564d7
--- /dev/null
+++ b/lib/config.cmake.in
@@ -0,0 +1,7 @@
+set(GAINPUT_VERSION @FBOW_VERSION@)
+
+@PACKAGE_INIT@
+
+include(${CMAKE_CURRENT_LIST_DIR}/gainputTargets.cmake)
+
+check_required_components(gainput)
\ No newline at end of file
